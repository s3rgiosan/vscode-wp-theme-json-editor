{
  "name": "vscode-wp-theme-json-editor",
  "displayName": "WP Theme JSON Editor",
  "description": "A form-driven visual editor for WordPress theme.json files",
  "version": "1.6.0",
  "publisher": "s3rgiosan",
  "license": "GPL-3.0-only",
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/s3rgiosan/vscode-wp-theme-json-editor"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Formatters"
  ],
  "keywords": [
    "wordpress",
    "theme.json",
    "theme",
    "json",
    "editor",
    "visual",
    "block theme"
  ],
  "activationEvents": [
    "onLanguage:json",
    "onLanguage:jsonc"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "wpThemeJsonEditor.open",
        "title": "Open in WP Theme JSON Editor",
        "category": "WP Theme JSON"
      },
      {
        "command": "wpThemeJsonEditor.openSidePanel",
        "title": "Open in WP Theme JSON Editor (Side Panel)",
        "category": "WP Theme JSON"
      },
      {
        "command": "wpThemeJsonEditor.openTab",
        "title": "Open in WP Theme JSON Editor (Tab)",
        "category": "WP Theme JSON"
      },
      {
        "command": "wpThemeJsonEditor.save",
        "title": "Save",
        "category": "WP Theme JSON"
      },
      {
        "command": "wpThemeJsonEditor.newBlockStyleVariation",
        "title": "New Block Style Variation",
        "category": "WP Theme JSON"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceFilename == 'theme.json' || (resourceExtname == '.json' && resourcePath =~ /[\\\\/]styles[\\\\/]/) || (wpThemeJsonEditor.hasCustomPatterns && resourceExtname == '.json')",
          "command": "wpThemeJsonEditor.open",
          "group": "navigation@100"
        }
      ]
    },
    "configuration": {
      "title": "WP Theme JSON Editor",
      "properties": {
        "wpThemeJsonEditor.defaultLayout": {
          "type": "string",
          "enum": [
            "sidePanel",
            "tab"
          ],
          "default": "tab",
          "description": "Default layout when opening theme.json files."
        },
        "wpThemeJsonEditor.showExperimentalByDefault": {
          "type": "boolean",
          "default": false,
          "description": "Show experimental and undocumented properties by default."
        },
        "wpThemeJsonEditor.includePatterns": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "**/theme.json",
            "**/styles/**/*.json"
          ],
          "markdownDescription": "Glob patterns for files the editor can open. A file is editable if its path matches any pattern. Defaults cover `theme.json` and style variations under `styles/`; add patterns to cover partial theme JSON (e.g. `**/src/theme-json/**/*.json`). Note: the Explorer right-click menu uses a fixed rule and only appears for `theme.json` and `styles/**`; for other locations use the Command Palette or keyboard shortcut."
        },
        "wpThemeJsonEditor.schemaVersion": {
          "type": "string",
          "enum": [
            "auto",
            "6.6",
            "6.7",
            "trunk"
          ],
          "default": "auto",
          "description": "Schema version to use. 'auto' detects from the $schema field in theme.json."
        }
      }
    },
    "keybindings": [
      {
        "command": "wpThemeJsonEditor.open",
        "key": "ctrl+shift+t",
        "mac": "cmd+shift+t",
        "when": "wpThemeJsonEditor.activeFileEditable"
      },
      {
        "command": "wpThemeJsonEditor.save",
        "key": "ctrl+s",
        "mac": "cmd+s",
        "when": "activeWebviewPanelId == 'wpThemeJsonEditor'"
      }
    ]
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:ext": "node esbuild.mjs --watch",
    "dev:webview": "npm run dev -w @s3rgiosan/theme-json-editor-ui",
    "dev": "concurrently \"npm:dev:ext\" \"npm:dev:webview\"",
    "build:ext": "node esbuild.mjs --production",
    "build:webview": "npm run build -w @s3rgiosan/theme-json-editor-ui",
    "build": "npm run build:ext && npm run build:webview",
    "check-types": "tsc --noEmit",
    "lint": "eslint src/ && npm run check-types",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run && npm run test -w @s3rgiosan/theme-json-editor-ui",
    "test:watch": "vitest",
    "vscode:prepublish": "npm run build",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "concurrently": "^8.2.2",
    "esbuild": "^0.24.0",
    "eslint": "^9.39.4",
    "minimatch": "^10.2.5",
    "typescript": "^5.3.3",
    "typescript-eslint": "^8.57.1",
    "vitest": "^4.1.0"
  }
}
