name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Install webview dependencies
        run: cd webview && npm ci

      - name: Type-check root
        run: npm run lint

      - name: Type-check webview
        run: cd webview && npm run lint

      - name: Test root
        run: npm test

      - name: Test webview
        run: cd webview && npm test

      - name: Build
        run: npm run build
